# TalentLink

## Overview

TalentLink is an AI-powered talent ecosystem designed to connect early-career candidates (interns, graduates) with employers through intelligent matching. It also enables educational institutions to track the placement of their students in real-time. The project aims to streamline recruitment for entry-level positions and provide valuable insights into talent pipelines and hiring trends.

## User Preferences

I prefer concise and direct communication. When making changes, prioritize iterative development and ask for confirmation before implementing major architectural shifts or significant feature alterations. Ensure all code adheres to modern TypeScript practices and is well-documented.

## System Architecture

TalentLink is built as a monorepo using pnpm workspaces. The system's API specification is defined in `lib/api-spec/openapi.yaml`, serving as the single source of truth for all API contracts and generating Zod validators.

The core components include:
- **API Server**: An Express 5 application with Drizzle ORM, structured logging (pino), and domain-split routes. Business logic, including the matching algorithm, is encapsulated.
- **Database**: PostgreSQL database managed by Drizzle ORM, with a schema split by domain.
- **API Client**: React Query hooks generated by Orval from the OpenAPI spec for frontend consumption.
- **Web App (`artifacts/talent-platform`)**: A React application built with Vite, utilizing wouter for routing, shadcn/ui for components, Recharts for data visualization, framer-motion for animations, and sonner for toasts. It features a real cookie-session authentication system with a "View as" demo fallback.
- **Mobile App (`artifacts/talent-mobile`)**: An Expo and React Native application providing a candidate-only experience (job browsing, application tracking, profile viewing). It reuses the same API client and backend as the web app.

**Key Architectural Decisions:**
- **Authentication**: `express-session` with `connect-pg-simple` manages sessions. User roles (`candidate`, `employer`, `institution`, `admin`) are central, with layered `org_role` for in-organization permissions (e.g., `owner`, `recruiter`, `super_admin`, `account_manager`). Secure password handling uses bcrypt.
- **Admin Console**: A dedicated dashboard (`/dashboard/admin/*`) provides comprehensive management capabilities, including user management, application oversight, and detailed analytics (hires, partner performance). Admin pages are protected by server-side middleware and client-side access control. Per-user actions on the candidates/employers/institutions admin pages allow toggling account status (`active`/`disabled` — disabled users cannot log in) and issuing a fresh password-reset link via a shared `AdminAccountActions` component (uses `GET /admin/accounts`, `PATCH /admin/users/:id/status`, `POST /admin/users/:id/reset-password`). Reset is atomic (token issuance + account lock in one transaction), revokes all existing sessions for the target user, and refuses self-action and admin-target action.
- **Account Managers**: The `account_manager` admin org-role owns a book of employer/institution accounts. The `employers` and `institutions` tables carry a nullable `account_manager_id` FK to `users`. When an account_manager calls `POST /admin/onboard`, the new org auto-assigns to them; only `super_admin` (treated as the default for null org-role admins) can reassign via `PATCH /admin/employers|institutions/:id/assign`. Account managers see a "My accounts" toggle on the employers/institutions admin lists (powered by `?mine=1`); admins see assignment metadata and a `<AccountManagerSelect>` (read-only badge for non-super-admins, Select dropdown for super-admins). The `/dashboard/admin/account-managers` page lists managers with employer/institution counts via `GET /admin/account-managers`. Super-admins can change another admin's org-role inline on the Team page via `PATCH /staff/:id/role`.
- **Customizable Roles & Permissions (admin + employer + institution)**: All three top-level roles share one machinery. The `admin_roles` table now carries `scope` (`'admin'|'employer'|'institution'`) plus nullable `employer_id` / `institution_id` FKs (cascade-delete to their parent org), with three partial unique indexes guaranteeing role-name uniqueness *within a scope+org*: admin rows have both org ids null, employer rows have `employer_id` non-null, institution rows have `institution_id` non-null. The permission catalog in `artifacts/api-server/src/lib/permissions.ts` exposes three sets — `PERMISSIONS` (admin), `EMPLOYER_PERMISSIONS`, `INSTITUTION_PERMISSIONS` — and `getUserPermissions(user)` dispatches by scope and filters by org id, returning the union of every permission granted to the user's `orgRole` (with `super_admin`, employer `owner`, and institution `owner` treated as implicit-all). `seedSystemRoles()` runs on every boot: it seeds the 6 admin system roles globally, then iterates every existing employer/institution and seeds (`owner`, `recruiter`, `viewer`) / (`owner`, `coordinator`, `viewer`) per-org, and drops any legacy null-org employer/institution rows. Owner-only `GET/POST/PATCH/DELETE /api/org-roles` (plus `GET /api/org-roles/permissions`) is the org-scoped CRUD API: every query hard-filters on the caller's `(scope, employer_id|institution_id)` via `callerCtx`/`scopeFilters`, so Employer A's owner gets a 404 trying to mutate Employer B's role. Admin's `GET/POST/PATCH/DELETE /admin/roles` is hardened to `scope='admin'` so it can never accidentally hit a per-org row. `staff.ts isValidOrgRole(topLevelRole, orgRole, orgId)` validates against the same scoped table so invite + role-change flows accept dynamic org roles. Server-side gating uses `requirePermission(<key>)` middleware on the most sensitive employer/institution endpoints (jobs CRUD, applications respond, students verify, org-profile edit), with `requireOrgOwner` reserved for org-roles CRUD itself. The frontend uses one shared `pages/dashboard/org-roles.tsx` (Dialog editor with grouped permission checkboxes, AlertDialog delete) wired at `/dashboard/admin/roles`, `/dashboard/employer/roles`, and `/dashboard/institution/roles`; the layout dropdown gates the "Team" link by `orgRole==='owner' || hasPermission('staff:manage')` and "Roles & permissions" by `orgRole==='owner'`. The Team page (`pages/dashboard/staff.tsx`) fetches `/api/org-roles` so custom roles immediately appear in invite/edit dropdowns. `AuthUser.permissions: string[]` (in the OpenAPI spec and `/auth/me` payload) drives `useAuth().hasPermission(key)` for client-side nav filtering.
- **Candidate Self-Onboarding & Verification Notifications**: Candidates self-register without admin approval — `POST /auth/register` for `role=candidate` immediately creates the candidate row, marks the user `active`, and starts a session, so the signup page redirects them straight to `/dashboard/candidate`. Employers and institutions still go through the original pending/approval flow. Institutions verify or unverify each `candidate_institutions` link (`PATCH /institutions/me/candidates/:id/verify`); verified status drives a green "Verified" pill (BadgeCheck lucide icon) per institution chip on the candidate detail page. Verification events insert into a new `notifications` table (`userId`, `kind`, `title`, `body`, `link`, `readAt`, `createdAt`); `notifyCandidateAboutVerification` fires from the institutions router. A `<NotificationBell />` in the global header polls `GET /notifications/unread-count` every 30s and lazy-loads `GET /notifications` on open; clicks fire `POST /notifications/:id/read` and "Mark all read" hits `POST /notifications/mark-all-read`. All notifications endpoints scope to `req.currentUser.id`.
- **Website Builder**: A `site_content` key-value store allows administrators to edit home-page copy and image URLs, with public API access and admin-only bulk upsert functionality (gated by the `site-content:edit` permission).
- **Universal User Profile + Avatar Upload**: Every user (candidate, employer, institution, platform admin/staff) edits the same profile via `/account/profile`. The `users` table carries `avatar_url`, `phone`, `title`, `bio` (all nullable text). `PATCH /api/auth/me/profile` (auth-required) accepts any subset of `fullName`/`phone`/`title`/`bio`/`avatarUrl`, supports `null` to clear nullable fields, and rejects `avatarUrl` values that are not normalized object paths matching `^/objects/[A-Za-z0-9._/-]+$`. Object-storage routes live in `artifacts/api-server/src/routes/storage.ts`: `POST /storage/uploads/request-url` (auth-required, image content-type allowlist + 10 MB cap) mints a presigned upload URL and returns its normalized object path; `GET /storage/objects/*` (auth-required) streams the object back. The web app uses `@workspace/object-storage-web`'s `useUpload` hook to PUT the file directly to the presigned URL, then persists the returned `objectPath` via `PATCH /me/profile`. The header avatar in `layout.tsx` and the profile page render avatars via `/api/storage{objectPath}` and fall back to the user's initial when no avatar is set.
- **Matching Algorithm**: Calculates a score based on skill coverage (65%), experience scaling (15%), and talent score (20%), clamped between 15-99.
- **UI/UX**: Emphasizes a clean, modern aesthetic using shadcn/ui, a consistent emerald/teal color palette with a dark mode variant, and lucide icons. No emojis are used.
- **Data Model**: Core entities include `institutions`, `employers`, `candidates`, `jobs`, and `applications`, with detailed sub-models for education, experience, and certifications. `candidate_institutions` handles many-to-many relationships, supporting multiple affiliations.

## External Dependencies

- **PostgreSQL**: Primary database for all application data.
- **Express.js**: Web application framework for the API server.
- **React**: Frontend library for both web and mobile applications.
- **Vite**: Build tool for the web application.
- **Expo/React Native**: Framework for the mobile application.
- **Drizzle ORM**: TypeScript ORM for database interaction.
- **shadcn/ui**: Reusable UI components for the web app.
- **Recharts**: Charting library for data visualization in the admin console.
- **framer-motion**: Animation library for the web app.
- **pino**: Structured logging library.
- **connect-pg-simple**: PostgreSQL session store for `express-session`.
- **bcrypt**: Password hashing library.
- **Orval**: API client generator from OpenAPI specifications.
- **Resend (currently stubbed)**: Intended email delivery service for authentication links and notifications.